# Web
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9
websockets>=12.0

# Core ML (CRITICAL)
numpy==1.26.4
torch==2.2.1
torchaudio==2.2.1

# Hugging Face
transformers==4.36.2
accelerate==0.26.1
huggingface_hub==0.20.3

# Audio
librosa==0.10.1
soundfile==0.12.1
audioread==3.0.1
noisereduce==3.0.0
openai-whisper

# Config / Validation
pydantic==2.6.4
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Database
sqlalchemy
aiosqlite

# Named Entity Recognition (NER)
spacy>=3.7.0
scispacy>=0.5.3

# EHR/FHIR Integration
httpx>=0.27.0

# HIPAA Encryption at Rest
cryptography>=42.0.0

# RAG (Retrieval-Augmented Generation)
chromadb>=0.5.0
sentence-transformers>=3.0.0  # includes CrossEncoder for reranking

# Auth & Security (Phase 4)
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
pyotp>=2.9.0
qrcode[pil]>=7.4

# PHI Detection (Phase 1 - NER-based)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Database Migrations
alembic>=1.13.0

# OAuth2/OIDC (Phase 1)
authlib>=1.3.0

# Infrastructure (Phase 8)
asyncpg>=0.29.0           # PostgreSQL async driver
redis[hiredis]>=5.0.0     # Redis cache + task queue
pyngrok>=7.0.0            # Colab ngrok tunneling
bitsandbytes>=0.43.0      # Model quantization (optional, GPU only)
